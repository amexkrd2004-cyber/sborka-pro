# СборкаПро — проектный журнал и памятка

Этот файл — **опорная точка для работы над проектом** и для ИИ-ассистента в новых сессиях.  
**Перед началом работы в чате:** кратко напомнить «читай `sborka.md`» или приложить файл — так не нужно каждый раз заново описывать контекст.

---

## 1. О проекте

| Поле | Значение |
|------|----------|
| Название | СборкаПро (условное) |
| Назначение | Android-приложение для кладовщиков: сборка заказов, скан ШК, смена статусов в МойСклад, печать стикеров; веб-панель администратора; сервер (webhook + API). |
| Источник требований | `ТЗ_СборкаПро.docx` в корне репозитория |
| Интеграции | **МойСклад** (REST API, webhooks), **amoCRM** (синхронизация через виджет **amgroup**, уже настроена и работает) |

---

## 2. Что уже есть у заказчика (на старте)

- Учётные записи и рабочие процессы в **МойСклад** и **amoCRM**.
- **Интеграция МойСклад ↔ amoCRM** (amgroup) настроена и проверена в бою.

**Уже не «будущий» сервер:** webhook **МойСклад → Railway** настроен и проверен (`assemblyMatch` при статусе «Сборка»). **Ещё не считать готовым без отдельной настройки:** доп. поля заказа в МойСклад под ТЗ, тестовые кладовщики в приложении — см. фазы B–C.

---

## 3. Целевой стек (из ТЗ)

- Мобильное приложение: **React Native (Expo)**, Android APK (EAS Build).
- Сервер: **Node.js + Express** (Railway / Render / VPS).
- БД: **SQLite** (старт) или **PostgreSQL**.
- Push: **Expo Push Notifications**.
- Сканер: **expo-barcode-scanner**.
- Печать: **ESC/POS** (термопринтеры Bluetooth/Wi-Fi); Zebra ZPL — опционально позже.
- Веб-панель: **React SPA** на том же сервере (или отдельный деплой — уточнить при реализации).

### 3.1. Структура кода в репозитории

| Путь | Содержимое |
|------|------------|
| `server/` | Backend: Node.js + Express. Вход `server/src/index.js`, БД `server/src/db/`, webhook `server/src/routes/webhook.js`, auth `server/src/routes/auth.js`, заказы `server/src/routes/orders.js`, МойСклад `server/src/services/moyskladApi.js`, `moyskladWebhookWorker.js`, `moyskladOrders.js`. См. `server/README.md`, **`docs/moysklad-integration.md`**. |
| `mobile/` | *(планируется, фаза C)* Expo/React Native |
| `web/` | *(планируется, фаза E)* админ-панель |

---

## 4. План действий (поэтапно)

Порядок согласован с оценкой «один разработчик + ИИ»: сначала ядро без самых капризных частей.

### Фаза A — Инфраструктура и МойСклад

**Статус: завершена** (п.1–5). Продакшен: **Railway**, Root Directory `server`, HTTPS (`*.up.railway.app`). Следующий крупный шаг — **фаза B** (БД, auth, API заказов).

1. ~~Инициализировать репозиторий: `server/` (Node), позже `mobile/` (Expo), `web/` (админка).~~ ✅
2. ~~Поднять сервер с HTTPS-публичным URL (Railway/Render/VPS + домен или выданный URL).~~ ✅ Railway, домен платформы, `GET /health` снаружи.
3. ~~В МойСклад: **токен API**, проверка доступа к заказам и складам.~~ ✅ Токен в **Variables** (`MOYSKLAD_TOKEN`); worker догружает заказ и статус по API.
4. ~~Настроить **webhook** «изменение заказа покупателя» → наш endpoint; фильтр: переход в статус **«Сборка»** (как в ТЗ).~~ ✅ Вебхуки **CREATE** + **UPDATE** для `customerorder` (JSON API); в коде сравнение с `MOYSKLAD_ASSEMBLY_STATE_NAME` (по умолчанию «Сборка»); имя статуса при необходимости через **GET `state.meta.href`** (в ответе заказа часто нет `state.name`). Регистрация вебхуков: `npm run moysklad:register-webhooks` / шаг 6 в `docs/railway-sborka-pro.md`.
5. ~~Логирование входящих webhook (без секретов в логах) для отладки.~~ ✅ Краткое резюме; полное тело — только при `WEBHOOK_LOG_FULL_BODY=true`; в логах — `worker queued`, `[moysklad-worker]`, `assemblyMatch`.

### Фаза B — Минимальный бэкенд

**Прогресс (2026-05-04):** PostgreSQL, схема таблиц, **`POST /auth/login`**, **`GET /auth/me`**, **`POST /auth/register-token`**, **`GET /orders`**, **`GET /orders/:id`**, **`POST /orders/:id/claim`** (атомарный захват). Осталось: **`PATCH /orders/:id/status`** (обновление статуса в МойСклад), журнал уведомлений, доработка ролей/складов по ТЗ.

1. ~~Таблицы: пользователи (кладовщики), привязка к складам, push-токены, журнал уведомлений, `assembly_log`, отказы, настройки (по ТЗ).~~ ✅ Первый набор в `server/src/db/schema.sql` (users, assembly_claims, push_tokens, assembly_log, order_refusals, app_settings); сид админа `npm run db:seed`.
2. ~~`POST /auth/login`, `POST /auth/register-token`, `GET /orders`, `GET /orders/:id`~~ ✅ **`PATCH /orders/:id/status`** — пока заглушка 501 (обновление в МойСклад — следующий шаг).
3. ~~Защита **«кто первый взял заказ»** — атомарное назначение~~ ✅ `POST /orders/:id/claim` + `ON CONFLICT DO NOTHING` в PostgreSQL.

### Фаза C — Мобильное приложение (MVP)

1. Экран входа, список заказов в статусе «Сборка», карточка заказа.
2. Сканер ШК и отметка товаров; кнопки **Собрано** / **Отгружено** / **Выдано клиенту** по логике ТЗ.
3. Push при появлении заказа на складе кладовщика.
4. Сборка **development/EAS** — не Expo Go из-за нативных модулей (печать).

### Фаза D — Печать

1. Выбрать 1–2 **эталонные** модели принтера для первой версии.
2. Подключение BT/Wi-Fi, макет стикера 80 мм (ТК, трек, ШК, примечание).

### Фаза E — Веб-панель администратора

1. Авторизация админа, дашборд, отказы сборки, пользователи и склады.
2. Аналитика и Excel — после стабилизации ядра (можно второй итерацией).
3. WebSocket для «колокольчика» — можно отложить после базовых экранов с опросом/обновлением.

### Фаза F — Ввод в эксплуатацию

1. Обновить **руководства** под фактические URL, имена полей МойСклад, скриншоты (не откладывать на конец — см. раздел **5.1** по ходу фаз C–E).
2. Пилот на одном складе, затем раскатка на 3–5 складов.

---

## 5. Структура документации

| Путь | Назначение |
|------|------------|
| `sborka.md` | Этот файл: контекст, план, решения, changelog |
| `docs/rukovodstva/kladovshik.md` | Кладовщик: установка APK, ежедневная работа |
| `docs/rukovodstva/administrator.md` | Администратор: веб-панель, пользователи, отказы, отчёты |
| `docs/rukovodstva/systemnyy-administrator.md` | Системный администратор: развёртывание с нуля, env, webhook, бэкапы |
| `server/README.md` | Запуск backend локально, эндпоинты, настройка вебхука МойСклад |
| `docs/railway-sborka-pro.md` | Пошаговая установка backend на Railway (GitHub, Root Directory `server`, домен, Variables, **шаг 7** — Postgres + JWT + сид) |
| `docs/moysklad-integration.md` | **Схема связи с МойСклад:** вебхуки CREATE/UPDATE, тело POST, токен, асинхронная догрузка заказа, статус «Сборка» |

### 5.1. Актуализация руководств (по мере роста проекта)

Сейчас в `docs/rukovodstva/*.md` много **нейтральных** формулировок («обычно», «вроде», «раздел вроде…») — это нормально для черновика. По мере появления реального UI, URL и процессов их нужно **заменять на конкретные**: точные названия кнопок, путей в меню, полей МойСклад, адресов.

**Правило:** любое изменение, которое видят пользователи или сисадмин при развёртывании, **в том же цикле** (тот же PR, та же задача или сразу следующим коммитом) отражается в соответствующем руководстве. Не копить «потом допишем».

**Кому что править (ориентир):**

| Изменили в проекте | Обновить |
|--------------------|----------|
| Экраны, сценарии, кнопки, push, сканер, печать в APK | `docs/rukovodstva/kladovshik.md` + скриншоты |
| Веб-панель: разделы, отказы, пользователи, отчёты, дашборд | `docs/rukovodstva/administrator.md` + скриншоты |
| Env, деплой, webhook, БД, EAS, домены, команды, восстановление | `docs/rukovodstva/systemnyy-administrator.md` |
| План фаз, стек, важные решения | `sborka.md` (и при необходимости — все руководства) |

**Версия документа:** внизу каждого руководства указаны *Версия документа* и статус. При существенной правке поднять минорную версию (0.1 → 0.2) и кратко описать в журнале ниже.

**Для сессий с ИИ:** в конце задачи можно явно попросить: *«Актуализируй руководства по разделу 5.1 в `sborka.md`»* — тогда правки войдут в `docs/rukovodstva/` и при необходимости в журнал раздела 7.

---

## 6. Решения и договорённости

*(Заполнять по мере работы. Формат: дата — решение.)*

- **2026-05-03** — Зафиксированы: ведение `sborka.md`, три пользовательских руководства в `docs/rukovodstva/`. Интеграция amocrm–МойСклад уже работает; наш сервер и приложение — в разработке.
- **2026-05-03** — Договорённость: при каждом изменении функционала для пользователей или развёртывания — сразу актуализировать соответствующее руководство (матрица в разделе 5.1); нейтральные формулировки поэтапно заменять на фактические.
- **2026-05-03** — Старт фазы A: создан каталог `server/` (Express, `/health`, `/webhook/moysklad`, безопасное логирование тела).
- **2026-05-03** — Интеграция МойСклад: зафиксирована в **`docs/moysklad-integration.md`** (2 вебхука, формат `events`, `MOYSKLAD_TOKEN`, асинхронный worker); код: `moyskladApi.js`, `moyskladWebhookWorker.js`.
- **2026-05-04** — **Фаза A закрыта на проде:** вебхуки доставляются, HTTP 200; worker с токеном на Railway; **`assemblyMatch: true`** при статусе «Сборка» (догрузка имени статуса по `state.meta.href`). Заглушка «TODO phase B» — сигнал к разработке фазы B, а не к доработке A.
- **2026-05-04** — **Старт фазы B:** PostgreSQL (`DATABASE_URL`), JWT (`JWT_SECRET`), схема в `server/src/db/schema.sql`, эндпоинты `/auth/*`, `/orders`, захват заказа `POST /orders/:id/claim`. Прод без БД не обязателен: без `DATABASE_URL` сервер остаётся в режиме A.

---

## 7. Журнал изменений (для ИИ и автора)

Правило: **при смене архитектуры, URL, статусов МойСклад, версий, важных команд или существенной правке руководств** — добавлять строку снизу.

| Дата | Изменение |
|------|-----------|
| 2026-05-03 | Создан `sborka.md`, черновики руководств (`kladovshik`, `administrator`, `systemnyy-administrator`), папка `docs/rukovodstva/screenshots/` для будущих скриншотов; в руководствах — вставки `![подпись](screenshots/файл.png)`. |
| 2026-05-03 | Раздел **5.1**: правило актуализации руководств (нейтральные формулировки → конкретика по мере роста проекта), матрица «изменение → какой файл править». |
| 2026-05-03 | **Фаза A:** добавлен `server/` (Node 18+, Express, `GET /health`, `POST /webhook/moysklad`, `dotenv`, редaction в логах). Корневой `.gitignore`. Обновлены `systemnyy-administrator.md` (запуск) и таблица документов. |
| 2026-05-03 | Добавлен **`docs/railway-sborka-pro.md`** — пошаговая инструкция Railway; обновлены `systemnyy-administrator.md`, таблица в разделе 5. |
| 2026-05-03 | `railway-sborka-pro.md` v0.2: блок про Windows — не вставлять `` ``` `` в PowerShell; установка Git и `git --version`. |
| 2026-05-03 | `railway-sborka-pro.md` v0.3: подробный шаг 5 — зачем GitHub, пустой репозиторий, клики в UI, случай с уже созданным README. |
| 2026-05-03 | `railway-sborka-pro.md` v0.4: проверка привязки после шага 6 (`remote -v`, `branch -vv`, `status`, браузер); URL в примере — плейсхолдер. |
| 2026-05-03 | `railway-sborka-pro.md` v0.5: типичная ошибка — незакрытая `"` в `git commit -m`, промпт `->`. |
| 2026-05-03 | `railway-sborka-pro.md` v0.6: опечатка `it push` → `git push`. |
| 2026-05-03 | `railway-sborka-pro.md` v0.7: настройка `git config` при Author identity unknown. |
| 2026-05-03 | `railway-sborka-pro.md` v0.8: `it config` vs `git config`, ошибка PowerShell про `It`. |
| 2026-05-03 | `railway-sborka-pro.md` v0.9: зависание `git push` — авторизация, PAT, `--verbose`. |
| 2026-05-03 | Код запушен на GitHub (`sborka-pro`), фаза A: переход к деплою Railway. |
| 2026-05-03 | Railway: успешный деплой после Root Directory `server` (диагноз / Set root directory). `railway-sborka-pro.md` v1.0 — где искать «сервис». |
| 2026-05-03 | МойСклад: вебхуки только через JSON API (`POST /entity/webhook`); обновлены `railway-sborka-pro.md` v1.1, `systemnyy-administrator.md`, `server/README.md`. |
| 2026-05-03 | **`docs/moysklad-integration.md`**, worker догрузки заказа, `MOYSKLAD_ASSEMBLY_STATE_NAME`; Railway шаг 6 — два вебхука + Variables. |
| 2026-05-03 | `railway-sborka-pro.md` v1.3: шаг 6 упрощён (зачем публичный URL, чеклист А–Е, `$myServer`). |
| 2026-05-04 | **Закрытие фазы A (прод):** push `84119b8` — снимок тела вебхука для worker, лог `worker queued`, скрипт `moysklad:register-webhooks`, правки доков. Push `2ec5065` — чтение имени статуса заказа через GET `state.meta.href` (исправление пустого `state.name` в API). В логах подтверждены `assemblyMatch: true` и `TODO phase B`. |
| 2026-05-04 | В `sborka.md` отмечено завершение фазы A (п.1–5), обновлены раздел 2, памятка для ИИ (раздел 8). |
| 2026-05-04 | **Фаза B (часть 1):** зависимости `pg`, `jsonwebtoken`, `bcryptjs`; `initDb` + схема; `POST /auth/login`, `GET /auth/me`, `POST /auth/register-token`; `GET /orders`, `GET /orders/:id`, `POST /orders/:id/claim`; `PATCH /orders/:id/status` → 501; `npm run db:seed`; обновлены `server/README.md`, `.env.example`, план в `sborka.md`. |
| 2026-05-04 | **`docs/railway-sborka-pro.md` v1.6:** шаг 7 — подробная инструкция для новичка (Postgres, `DATABASE_URL`, `JWT_SECRET`, сид, проверки curl); строки в «Типичные проблемы». |

---

## 8. Памятка для новой сессии с ИИ

Скопируйте в чат при необходимости:

> Проект СборкаПро: контекст и план в файле `sborka.md` (актуализация руководств — раздел 5.1). Backend: `server/README.md`, МойСклад: `docs/moysklad-integration.md`, деплой: `docs/railway-sborka-pro.md`. Руководства в `docs/rukovodstva/`. ТЗ в `ТЗ_СборкаПро.docx`. МойСклад ↔ amoCRM (amgroup) уже связаны. **Фаза A завершена** (Railway HTTPS, вебхуки, worker, `assemblyMatch` для «Сборки»). **Фаза B в работе:** PostgreSQL + JWT + `/orders` + `claim` готовы; дальше — **PATCH статуса в МойСклад** и прочее по ТЗ. Продолжаем: [уточнить шаг].

---

*Последнее обновление файла: 2026-05-04*
